# 设计决策记录

记录已经拍板的选择及其理由。来源是实施初期与需求方的问答，这里只保留结论，不保留讨论过程。

## 数据库

**选型：SQL Server，兼容级别 100。** 目标运行环境是 SQL Server 2008 R2，因此 `Database:CompatibilityLevel`
默认 100，EF 也按该级别配置（`Program.cs`）。MySQL 不在本期范围内。

> 注意：SQL Server 2008 R2 已于 2019 年结束支持。新部署应使用受支持的版本，把
> `Database:CompatibilityLevel` 调到 110/120/130 等，代码不需要改动。

## 人物与草稿

**草稿只存在于 `FamilyTree_PersonDraft`，正式表 `FamilyTree_Person` 只放已入档的人。**
族人先在草稿里填写，入档时才写正式表；正式表不保留 draft 状态。

**每个人先建自己的小树，匹配上主谱后才链入。** 在链入生效之前，他填的内容是一棵独立的个人树，
不进入主谱、不对其他族人可见。

## 分支

**不存在真正意义上的「分支」实体。** 「支链管理员」是一个权限概念，含义是：
可以向上补父辈、向下补子女。它不对应任何部门，也不要求持有者属于某个部门岗。

## 族人可填范围

**本人、父母、子女、兄弟姐妹、祖父母；已认证的族人还可以填父母的兄弟姐妹（用于旁系串链）。**
填写本身不受限，但只有匹配上主谱并通过审批，这些人才会成为主谱的一部分。

## 主谱的根

**由族谱管理员确定，且根会随录入不断上移。** 主谱不一定有一个天然的根：
不同支系各自向上补录之后，根会继续延伸。因此系统按「无父边者即当前根」动态计算，
而不是把某个人固定成根。

## 同一人匹配

**匹配依据：本人姓名 + 父母姓名，出生年月作为加强项。**
同一个人只允许链入主谱一次；两个尚未链入主谱的分支是彼此独立的。
链入时若向上世系不一致，交由超管在冲突单里确认。

## 登录与身份

**微信登录，或身份证号登录。** 身份证只存加盐 SHA-256 哈希（`FamilyTree:IdCardHashSalt`），不存明文。
手机号可选填，不强制。

> 因此 `IdCardHashSalt` 必须在部署时改成本部署专属的随机值——它一旦更换，
> 已有的身份证哈希会全部失配。生产环境启动时会校验该值不是默认值，否则拒绝启动。

## 字辈

**本期只做查询，不做全族字辈表的维护流程。** 世代号由录入人手工填写，不按树自动推算。

## 其他

- **纸质谱版式**：本期不做。
- **历史数据导入**：本期不需要。
- **小程序主体与 HTTPS 域名**：从配置文件读取（`FamilyTree:WeChat`），仓库内不含任何主体信息或密钥。
