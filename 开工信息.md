# 开工信息

只改本文件，不要改 `base/开发新应用系统_提示词模板.md`。

改完后：把客户资料放入 `基本需求/`，用 Cursor 打开本工程根目录，把提示词文件发给 AI 即可开工。

## 必填

系统中文名：家族族谱系统

## 选填

其它约束：无

---

以下由文件夹名自动决定，一般不用写：

- 主目录名称 / 命名空间 / 数据库名 / AppCode / 业务表前缀 = 本工程根文件夹名
- 客户需求 = `基本需求/` 下除 `说明.md` 以外的文件
